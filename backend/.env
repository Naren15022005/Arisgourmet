DB_HOST=localhost
DB_PORT=3306
DB_USER=ag_user
DB_PASSWORD=ag_password
DB_NAME=arisgourmet
JWT_SECRET=dev_jwt_secret_change
REDIS_HOST=redis
REDIS_PORT=6379

# NOTE: This file contains local development secrets and should NOT be committed.
# The repo contains `env.example` with placeholders. When running in Docker,
# set DB_HOST=mysql or use the `infra` env file `backend/.env.docker`.
